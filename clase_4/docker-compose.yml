services:
  prueba:
    image: python:3.12-slim
    container_name: prueba_app
    working_dir: /app/prueba1
    volumes:
      - ./:/app
    ports:
      - "7676:7676"
    command: >
     sh -c "pip install -r requirements.txt && python manage.py runserver 0.0.0.0:7676"
